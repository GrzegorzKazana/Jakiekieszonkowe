const API_DELAY = 50;

export const dictionaryChange = data => {
  console.log(data);
  //append id to new dictionary row
  //data should specify dictionary in question, or create separate functions for each dicts
  //return if row was valid
  //if yes, send back new dict
};

export const dictionaryRowDelete = id => {
  console.log(id);
  //data should specify dictionary in question, or create separate functions for each dicts
  //return if row was valid
  //if yes, send back new dict
};
